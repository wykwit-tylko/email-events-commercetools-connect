# Follow-Up Tasks

Long-term tasks to revisit after the current Event Proxy and Email Worker shape is settled.

1. Update templates for selected emails: account verification, password reset, order update.
2. Add deployment scripts that read `.env` and publish the Connect app to commercetools. A template exists for this.
